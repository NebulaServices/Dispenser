generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  id      String @id
  usage   Int    @default(3)
  domains Json   @db.Json
}

model domains {
  name String @id
}
